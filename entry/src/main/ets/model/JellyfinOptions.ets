export interface ConfigInterface
{
    server?: string;
    username?: string;
    password?: string;
    accessToken?: string;
    deviceId?: string;
    userId?: string;
    serverVersion?: string;
}